gitea:
  admin:
    username: "shanks"
    password: "Sp4rti4te!$"
    email: "admin@lab.local"
  config:
    database:
      DB_TYPE: postgres
      HOST: "10.10.1.30:5432"
      NAME: gitea
      USER: dbadmin
      PASSWD: "Sp4rti4te!$"
    server:
      DOMAIN: gitea.lab.local
      ROOT_URL: "http://gitea.lab.local"
      HTTP_PORT: 3000
    session:
      PROVIDER: db
    cache:
      ADAPTER: memory
    queue:
      TYPE: level

postgresql:
  enabled: false

postgresql-ha:
  enabled: false

redis-cluster:
  enabled: false

persistence:
  enabled: true
  storageClass: nfs
  size: 10Gi

ingress:
  enabled: true
  className: traefik
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
  hosts:
    - host: gitea.lab.local
      paths:
        - path: /
          pathType: Prefix
