[defaults]
inventory = inventory/hosts.yml
private_key_file = ~/.ssh/k8s-lab
remote_user = debian
host_key_checking = False
stdout_callback = default
result_format = yaml
callbacks_enabled = timer

[privilege_escalation]
become = True
become_method = sudo
become_user = root
